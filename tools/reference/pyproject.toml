[project]
name = "orangecount-reference"
version = "0.0.0"
description = "Development-only Beancount v3 differential-test environment for OrangeCount"
requires-python = ">=3.11"
dependencies = [
  "beancount>=3,<4",
]

[tool.uv]
package = false
