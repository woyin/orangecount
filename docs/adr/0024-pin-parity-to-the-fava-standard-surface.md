# Pin parity to the Fava standard surface

OrangeCount will target all built-in local-interface capabilities of one explicitly pinned Fava release, using the locally running reference instance to establish its version and acceptance evidence. Third-party plugin pages, user-defined extensions, and Fava's HTTP API remain excluded; this makes full UX parity a finite, testable product scope without taking on the unbounded Fava extension ecosystem.
