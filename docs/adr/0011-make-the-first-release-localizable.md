# Make the first release localizable

OrangeCount's first release will externalize user-facing interface and diagnostic messages into translation catalogs rather than hard-coding a single language. Display locale is explicitly selected and separated from ledger semantics, so localized presentation never changes parsing, ordering, valuation, or accounting calculations.
