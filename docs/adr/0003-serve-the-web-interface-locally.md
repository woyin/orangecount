# Serve the web interface locally

The built-in web interface will be launched by the OrangeCount CLI and bind to localhost only. OrangeCount is a single-user personal tool, so local serving avoids authentication, hosted deployment, and remote-access security concerns while retaining Fava's browser workflow.
