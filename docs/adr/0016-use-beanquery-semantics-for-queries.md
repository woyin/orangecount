# Use BeanQuery semantics for queries

OrangeCount's CLI and web query workbench will use the current BeanQuery semantics used by Fava. It will diagnose and suggest migration for recognizable legacy Beancount query differences rather than maintaining two query engines with divergent columns, functions, and results.
