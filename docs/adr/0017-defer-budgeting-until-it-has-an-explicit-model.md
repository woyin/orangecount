# Defer budgeting until it has an explicit model

The first OrangeCount release will not include budgeting or introduce a private budget syntax. Budgeting in the existing ecosystem generally depends on plugins or specialized configuration, which conflicts with the chosen v3-core, no-plugin compatibility boundary; core-derived reports remain the first-release focus.
