# Defer external transaction imports

The first OrangeCount release will load, validate, and analyze existing `.bean` ledgers only; it will not include bank or broker CSV/OFX imports or automated categorization. Those integrations are institution-specific and will be designed later as separate modules once the compatible accounting core and read-only workflows are proven.
