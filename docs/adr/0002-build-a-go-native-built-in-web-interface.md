# Build a Go-native built-in web interface

OrangeCount will include a Go-native web interface covering Fava's essential personal-ledger workflows, rather than requiring Fava or integrating with its Python stack. A separate interface would preserve ecosystem interoperability but would leave the principal user workflow dependent on the system this project is intended to replace.
