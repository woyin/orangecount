# Run offline without telemetry

OrangeCount will make no runtime network requests, telemetry submissions, or automatic update checks. Operational diagnosis relies on local detailed logs; any future version check or diagnostic upload must be explicit opt-in and must never transmit ledger content, paths, transactions, queries, or raw diagnostics.
