# Use a three-layer UX parity gate

Each in-scope Fava capability must pass equivalent task and interaction behavior, high-fidelity visual structure and hierarchy, and automated visual regression with redacted deterministic fixtures at representative viewport sizes. Literal pixel identity is not required because browser font rasterization and an independent implementation create irrelevant differences; the gate instead makes meaningful UX regressions objective without treating screenshot noise as product failure.
