# Use English as the visual parity baseline

English Fava screens will be the deterministic visual baseline for automated parity regression. OrangeCount's `en` interface must meet the complete UX parity gate, while `zh-CN` preserves the same information architecture and interactions but may differ where translated copy and CJK font metrics legitimately change layout; this retains the product's bilingual commitment without treating localization as a visual defect.
