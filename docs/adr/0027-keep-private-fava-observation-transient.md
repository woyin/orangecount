# Keep private Fava observation transient

The locally running Fava instance may be exercised against its private ledger solely for transient, local UX observation. Screenshots, DOM captures, responses, account names, amounts, paths, and agent task payloads from that session must not be persisted or committed; repeatable parity evidence is instead derived from sanitized fixtures, preserving both high-fidelity UX discovery and ledger privacy.
